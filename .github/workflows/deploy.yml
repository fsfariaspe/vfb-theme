name: Deploy WordPress Theme - Nuvem Hospedagem

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: |
        npm install
        
    - name: Build assets
      run: |
        npm run build
        
    - name: Deploy to Nuvem Hospedagem
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        server-dir: ${{ secrets.FTP_DIRECTORY }}
        protocol: ftp
        port: 21
        timeout: 120000
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.github/**
          **/.vscode/**
          **/README*.md
          **/DEPLOY-SETUP.md
          **/package*.json
          **/webpack.config.js
          **/.gitignore
          **/DOCUMENTACAO*.md
          **/ESTRUTURA*.md
          **/INSTALACAO*.md
          **/referencias/**
